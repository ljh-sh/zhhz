# Build & Test Report: `v0.8.2`

_Generated: 2026-07-15T13:30Z (UTC)_

_Commit: `e8f3eec` on `refs/heads/main`_


Native build, test, and binary production on 3 platforms for upcoming release **v0.8.2**.

## Per-platform results

| Platform | OS runner | cargo test | perf_only built | diff_corpus built |
|--|--|--|--|--|
| macos-arm64 | `macos-latest` | ✅ 43 passed (6 binaries) | ✅ | ✅ |
| linux-amd64 | `ubuntu-latest` | ✅ 43 passed (6 binaries) | ✅ | ✅ |
| linux-arm64 | `ubuntu-24.04-arm` | ✅ 43 passed (6 binaries) | ✅ | ✅ |

## Verdict

**All 3 native platforms passed.** Safe to trigger build-and-release.

## What landed in v0.8.2

Bundles three sequential feature releases:

### v0.8.0 — detect TW/HK classifier tuning
- `滑鼠` (used in both Taiwan and Hong Kong) now correctly classifies as
  `cn-tw` instead of `cn-t`. Algorithm uses absolute phrase-character
  counts and defaults to TW for shared regional-traditional phrases.
- New test: `regional_shared_phrase_defaults_to_tw`.

### v0.8.1 — `--auto --target <REGION>`
- Configurable destination region for `--auto`. Default remains cn-s.
- Supports the common `from → target` pairs and emits clear errors for
  unsupported combos (e.g. `cn-tw → cn-t`).
- Validation: `--target` requires `--auto`; conflicts with `--to`.

### v0.8.2 — `zhhz info <CONFIG>`
- New introspection subcommand. Prints config name, description,
  source/target region, and a tiny input/output example for any of
  the 16 built-in OpenCC configs.
- Pure UI, no conversion performed.

## Compatibility

No conversion core change. All 16 OpenCC configs produce byte-identical
output to v0.7.7. Library API unchanged. WASM / npm / PyPI surfaces
unchanged.

## Next step

When this report is green, trigger the **build-and-release** workflow
with `next_version = v0.8.2`.